# WPGen Environment Configuration
# Copy this file to .env and fill in your actual values

# ============================================
# LLM Provider API Keys
# ============================================
# NOTE: If using local LLMs (local-lmstudio or local-ollama),
# you do NOT need any API keys below. Just set provider in config.yaml!

# OpenAI API Key
# Get your key from: https://platform.openai.com/api-keys
# Required if using OpenAI as your LLM provider (provider: "openai")
OPENAI_API_KEY=your_openai_api_key_here

# Anthropic API Key
# Get your key from: https://console.anthropic.com/
# Required if using Anthropic as your LLM provider (provider: "anthropic")
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Local LLM Providers (LM Studio / Ollama)
# No API keys needed! Just install LM Studio or Ollama and set:
#   - config.yaml: provider: "local-lmstudio" or "local-ollama"
# See README.md "Using Local LLMs" section for full setup instructions

# ============================================
# GitHub Configuration
# ============================================

# GitHub Personal Access Token
# Get your token from: https://github.com/settings/tokens
# Required scopes: repo, workflow
# Optional - only needed if you want to push themes to GitHub
GITHUB_TOKEN=your_github_token_here

# ============================================
# Deployment Configuration (Optional)
# ============================================

# FTP Settings
# Only needed if using FTP deployment
FTP_HOST=ftp.yourhost.com
FTP_USERNAME=your_ftp_username
FTP_PASSWORD=your_ftp_password

# SSH Settings
# Only needed if using SSH deployment
SSH_HOST=ssh.yourhost.com
SSH_USERNAME=your_ssh_username
SSH_PRIVATE_KEY=/path/to/your/private/key
SSH_REMOTE_PATH=/var/www/html/wp-content/themes

# ============================================
# WordPress Site Management (REST API)
# ============================================

# WordPress Site URL
# The base URL of your WordPress site (without /wp-json)
# Example: https://yourdomain.com or https://yourdomain.com/wordpress
WP_SITE_URL=https://example.com

# WordPress Username
# Your WordPress admin username
WP_USERNAME=admin

# WordPress Application Password
# Generate at: https://yoursite.com/wp-admin/profile.php
# Recommended over regular password for better security
# Format: xxxx xxxx xxxx xxxx xxxx xxxx (WordPress generates this)
# Remove spaces when pasting here
WP_APP_PASSWORD=xxxxxxxxxxxxxxxxxxxx

# Alternative: Regular WordPress Password (less secure)
# Only use if Application Passwords are not available
# WP_PASSWORD=your_wordpress_password

# ============================================
# Optional Settings
# ============================================

# Flask Secret Key (for web UI)
# Change this in production
FLASK_SECRET_KEY=change-me-in-production

# Flask Debug Mode (development only)
FLASK_DEBUG=false

# ============================================
# Advanced: Model and Provider Overrides
# ============================================
# These environment variables override config.yaml settings
# Useful for CI/CD or temporary changes without editing config

# Override LLM provider selection
# Options: openai, anthropic, local-lmstudio, local-ollama
# WPGEN_LLM_PROVIDER=openai

# Override OpenAI model
# WPGEN_OPENAI_MODEL=gpt-4-turbo-preview

# Override Anthropic model
# WPGEN_ANTHROPIC_MODEL=claude-3-5-sonnet-20241022

# Override Ollama brains model
# WPGEN_OLLAMA_MODEL=llama3.1:8b-instruct

# ============================================
# Gradio UI Configuration
# ============================================
# Gradio server host (use 127.0.0.1 for Windows)
# GRADIO_SERVER_NAME=0.0.0.0

# Gradio server port
# GRADIO_SERVER_PORT=7860
